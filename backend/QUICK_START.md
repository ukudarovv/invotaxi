# Быстрый старт - Инво Такси Backend

## 🚀 Запуск сервера

```bash
cd backend
.\venv\Scripts\Activate.ps1  # Windows
# или
source venv/bin/activate      # Linux/Mac

python manage.py runserver
```

Сервер будет доступен по адресу: **http://localhost:8000**

## 📝 Тестирование API

### 1. Получить список регионов (публичный endpoint)

```bash
curl http://localhost:8000/api/regions/
```

### 2. Запрос OTP кода

```bash
curl -X POST http://localhost:8000/api/auth/phone-login/ \
  -H "Content-Type: application/json" \
  -d "{\"phone\": \"+7 (777) 777-77-77\"}"
```

**Важно:** OTP код будет выведен в консоль Django сервера (для разработки).

### 3. Проверка OTP и получение токена

```bash
curl -X POST http://localhost:8000/api/auth/verify-otp/ \
  -H "Content-Type: application/json" \
  -d "{\"phone\": \"+7 (777) 777-77-77\", \"code\": \"123456\"}"
```

Замените `123456` на реальный код из консоли сервера.

### 4. Создание заказа (требует токен)

```bash
curl -X POST http://localhost:8000/api/orders/ \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -d '{
    "pickup_title": "ул. Ленина, 1",
    "dropoff_title": "ул. Пушкина, 10",
    "pickup_lat": 55.7558,
    "pickup_lon": 37.6173,
    "dropoff_lat": 55.7500,
    "dropoff_lon": 37.7000,
    "desired_pickup_time": "2024-12-02T09:00:00Z",
    "has_companion": false,
    "note": "Тестовый заказ"
  }'
```

### 5. Получение заказов

```bash
curl http://localhost:8000/api/orders/ \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN"
```

## 🧪 Автоматическое тестирование

Используйте скрипт `test_api.py`:

```bash
python test_api.py
```

## 📊 Тестовые данные

После выполнения `python manage.py load_mock_data` доступны:

- **5 регионов**: Северный, Южный, Центральный, Восточный, Западный
- **10 водителей**: первые 5 онлайн
- **51 пассажир**: включая тестового `+7 (777) 777-77-77`

## 🔑 Тестовый аккаунт

**Пассажир:**
- Телефон: `+7 (777) 777-77-77`
- Имя: Тестов Тест Тестович

**Водитель:**
- Телефон: `+7 (900) 100-10-10` (первый водитель)
- Или любой другой из списка водителей

## 🌐 WebSocket тестирование

Используйте инструменты типа:
- **Postman** (поддержка WebSocket)
- **wscat** (npm install -g wscat)
- **Python websocket клиент**

Пример подключения:
```
ws://localhost:8000/ws/orders/{order_id}/
```

С токеном в query параметрах или заголовках (зависит от реализации).

## 📚 Дополнительная документация

- `API_DOCUMENTATION.md` - полная документация API
- `INTEGRATION_GUIDE.md` - руководство по интеграции с Flutter
- `SETUP_BACKEND.md` - подробная инструкция по установке

## ⚠️ Важные замечания

1. **OTP коды** в режиме разработки выводятся в консоль Django
2. **База данных** - SQLite (для production используйте PostgreSQL)
3. **WebSocket** использует InMemoryChannelLayer (для production нужен Redis)
4. **CORS** настроен для localhost:3000 и localhost:8080

## 🐛 Troubleshooting

### Сервер не запускается
- Проверьте, что порт 8000 свободен
- Убедитесь, что виртуальное окружение активировано
- Проверьте, что все зависимости установлены

### Ошибки миграций
```bash
python manage.py makemigrations
python manage.py migrate
```

### Ошибки с базой данных
```bash
# Удалите базу и создайте заново
Remove-Item db.sqlite3
python manage.py migrate
python manage.py load_mock_data
```

