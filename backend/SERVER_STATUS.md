# Статус сервера

## ✅ Сервер запущен и работает!

**URL:** http://localhost:8000

**Статус:** 🟢 Активен

## Проверенные endpoints

### ✅ Регионы
- **GET** `/api/regions/` - Работает
- Возвращает 5 регионов

### ✅ Аутентификация
- **POST** `/api/auth/phone-login/` - Работает
- **POST** `/api/auth/verify-otp/` - Готов к тестированию

## Быстрый запуск

### Windows:
```bash
cd backend
START_SERVER.bat
```

### Linux/Mac:
```bash
cd backend
./START_SERVER.sh
```

### Или вручную:
```bash
cd backend
.\venv\Scripts\Activate.ps1  # Windows
python manage.py runserver
```

## Тестирование

### Простой тест регионов:
```bash
curl http://localhost:8000/api/regions/
```

### Тест OTP:
```bash
# 1. Запрос OTP
curl -X POST http://localhost:8000/api/auth/phone-login/ \
  -H "Content-Type: application/json" \
  -d "{\"phone\": \"+7 (777) 777-77-77\"}"

# 2. Проверьте консоль Django для OTP кода
# 3. Проверка OTP
curl -X POST http://localhost:8000/api/auth/verify-otp/ \
  -H "Content-Type: application/json" \
  -d "{\"phone\": \"+7 (777) 777-77-77\", \"code\": \"КОД_ИЗ_КОНСОЛИ\"}"
```

## Логи сервера

Проверьте консоль, где запущен `python manage.py runserver` для:
- OTP кодов (выводятся в консоль)
- Ошибок и предупреждений
- Запросов к API

## Следующие шаги

1. ✅ Сервер запущен
2. ✅ API работает
3. ⏭️ Интеграция с Flutter приложением
4. ⏭️ Тестирование WebSocket
5. ⏭️ Настройка production окружения

## Полезные команды

```bash
# Остановить сервер
Ctrl+C в консоли сервера

# Перезапустить
python manage.py runserver

# Проверить миграции
python manage.py showmigrations

# Создать суперпользователя
python manage.py createsuperuser
```

